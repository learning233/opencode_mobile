修复 cb444f4 提交检查中发现的三个问题（改动很小，均为局部修改）：

1. **lib/pages/home/prompt_input.dart** — 将残留的 `_LogSheet` / `_LogSheetState` 两个类（约 1772 行起）整体注释掉（不删除，保留代码备用），消除 `unused_element` 警告。注释块顶部加一行简短标记说明。

2. **lib/controllers/vcs_controller.dart** — 移除 `onInit` 中的启动即刷 `refreshAll()`（vcs_controller.dart:24）。项目扫描设置 `activeProject` 后 `ever` worker 会自动触发刷新，面板每次打开（initState）也会刷新，启动时这次请求是多余的且可能在未连接时提前触发全局 401 标志。项目切换 worker 与 `onClose` 清理保持不变。

3. **lib/widgets/vcs/vcs_branch_sheet.dart + lib/utils/translations.dart** — 为复制分支名提示新增翻译键 `vcsBranchCopied`（中文「已复制分支名」/ 英文 'Branch name copied'），将 vcs_branch_sheet.dart:151 的硬编码中文改为 `.tr` 取值，与本次提交已补全的 i18n 保持一致。

4. 运行 `flutter analyze` 验证无新增告警。

不改动的内容：`VcsInfo.isClean/branches`、`VcsStatusFile.staged` 冗余字段保留（防御性解析无害）；`refreshAll` 并发保护与「有更改」圆点实时刷新属已知限制，本次不动。